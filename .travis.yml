---

sudo: required

language: python
python: "2.7"
# install: pip install ansible-lint
services:
  - docker

before_install:
  - docker pull williamyeh/ansible:ubuntu16.04
  # - docker run -it pip install ansible-lint
  # - docker ps -a
  # - docker run williamyeh/ansible:ubuntu16.04 /bin/sh -c "cd /root; ansible-lint tasks/main.yml"
  # - docker run williamyeh/ansible:ubuntu16.04 /bin/sh -c "cd /root; ansible-playbook -i test/inventory --syntax-check --list-tasks test/test.yml"
  - docker run williamyeh/ansible:ubuntu16.04 ansible --version
  - docker run -it -v $(pwd):/home/cabot -w /home/cabot/ williamyeh/ansible:ubuntu16.04 ls -al
  - docker run williamyeh/ansible:ubuntu16.04 pip install ansible-lint
  - docker run williamyeh/ansible:ubuntu16.04 ansible-lint /home/cabot/tasks/main.yml

# script:
#   - pip install ansible-lint
#   - ansible-lint tasks/main.yml
#   - ansible-playbook -i test/inventory --syntax-check --list-tasks test/test.yml
#   - ansible-playbook -i test/inventory --connection=local --become -vvvv test/test.yml -u root
# # before_script:
#   - pip install ansible-lint
script:
  - ls -al
  # - ansible-lint tasks/main.yml
  # - ansible --version
 # - ansible-playbook -i test/inventory --syntax-check --list-tasks test/test.yml
  # - ansible-playbook -i test/inventory --connection=local --become -vvvv test/test.yml -u root
  # - >
  #     ansible-playbook -i test/inventory --connection=local --become -vvvv test/test.yml -u root
  #     | grep -q 'changed=0.*failed=0'
  #     && (echo 'Idempotence test: pass' && exit 0)
  #     || (echo 'Idempotence test: fail' && exit 1)

notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
